const EXTERNAL_GATES=new Set(['health_readiness','core_smoke','logs_5xx','rollback','csrf_idempotency','operational_health','nonfunctional','uat_signoff']);
const FORBIDDEN=/\b(staging|template|loopback|baseline|not[_ -]?run|pending)\b/i;
export function validateActualCutoverProvenance(evidence){const failures=[];if(evidence?.template!==false)failures.push('ACTUAL_EVIDENCE_MUST_NOT_BE_TEMPLATE');if(evidence?.activationState!=='actual')failures.push('ACTIVATION_STATE_NOT_ACTUAL');for(const gate of evidence?.gates||[]){if(EXTERNAL_GATES.has(gate.id)&&gate.status==='PASS'&&FORBIDDEN.test(gate.evidence||''))failures.push(`${gate.id}_NON_PRODUCTION_EVIDENCE`);}if(evidence?.productionGo===true&&failures.length)failures.push('PRODUCTION_GO_WITH_INVALID_PROVENANCE');return{status:failures.length?'FAIL_ACTUAL_EVIDENCE_PROVENANCE':'PASS_ACTUAL_EVIDENCE_PROVENANCE',failures,productionGo:false};}
